{
  "name": "serve_frontend",
  "version": "1.0.0",
  "description": "express-server to serve react-app",
  "main": "server.js",
  "scripts": {
    "start": "node server",
    "server": "nodemon server",
    "client": "npm start --prefix frontend",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "jbperidy",
  "license": "MIT",
  "dependencies": {
    "concurrently": "^6.0.0",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "express-http-proxy": "^1.6.2",
    "http": "^0.0.1-security",
    "morgan": "^1.10.0",
    "nodemon": "^2.0.7",
    "path": "^0.12.7"
  }
}
